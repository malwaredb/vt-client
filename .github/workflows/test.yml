name: Test
on: [ push, pull_request ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: cargo test
      uses: actions-rs/cargo@v1
      with:
        command: test
        args: --all-features
